import React from "react";

interface RightManagerProps {
  type?: string;
}

const RightManager: React.FC<RightManagerProps> = (props) => {
  return <div>RightManager</div>;
};

export default RightManager;
